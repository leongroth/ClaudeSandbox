# Resource Library Component

A production-ready React component for displaying and filtering a library of resources with comprehensive test coverage.

![Resource Library Screenshot](https://via.placeholder.com/800x400?text=Resource+Library+Component)

## Features

- 🔍 **Search Filtering** - Real-time search across titles and descriptions
- 🏷️ **Type Filtering** - Filter by White Papers, Infographics, or Videos
- 🎯 **Tag Filtering** - Multi-select tag filtering with OR logic
- 📅 **Date Filtering** - Filter resources by year
- 🔄 **Sorting** - Sort by relevance, date, or title
- ♻️ **Reset Filters** - Clear all filters with one click
- 📱 **Responsive Design** - Works on all screen sizes
- ♿ **Accessible** - ARIA labels and keyboard navigation
- ⚡ **Optimized** - Uses React.useMemo for efficient filtering

## 🚀 Quick Start - Run on Localhost

### 1. Clone the repository

```bash
git clone https://github.com/leongroth/ClaudeSandbox.git
cd ClaudeSandbox
```

### 2. Install dependencies

```bash
npm install
```

### 3. Start the development server

```bash
npm run dev
```

The app will automatically open in your browser at **http://localhost:3000** 🎉

### Other available commands:

```bash
# Build for production
npm run build

# Preview production build
npm run preview

# Run tests
npm test

# Run tests in watch mode
npm run test:watch

# Generate coverage report
npm run test:coverage
```

## Usage in Your Own Project

```tsx
import ResourceLibrary from './src/ResourceLibrary';

function App() {
  return <ResourceLibrary />;
}
```

## Test Coverage

This component includes **30 comprehensive tests** covering:

- ✅ Component rendering (all variants)
- ✅ User interactions (clicks, inputs, selections)
- ✅ Filter logic (search, type, tags, date)
- ✅ Combined filters with AND logic
- ✅ Empty states
- ✅ Accessibility features
- ✅ Edge cases

**Target Coverage:** 80%+ across all metrics (branches, functions, lines, statements)

## Project Structure

```
ClaudeSandbox/
├── public/
│   └── index.html              # HTML entry point
├── src/
│   ├── ResourceLibrary.tsx     # Main component
│   ├── index.tsx               # React entry point
│   ├── setupTests.ts           # Jest configuration
│   └── __tests__/
│       ├── ResourceLibrary.test.tsx
│       └── filteringLogic.test.ts
├── babel.config.js             # Babel configuration
├── jest.config.js              # Jest configuration
├── tsconfig.json               # TypeScript configuration
├── vite.config.ts              # Vite configuration
└── package.json                # Dependencies and scripts
```

## Component API

### Resource Interface

```typescript
interface Resource {
  id: string;
  type: 'White Paper' | 'Infographic' | 'Video';
  title: string;
  description: string;
  url: string;
  tags: string[];
  date?: string;
}
```

### FilterState Interface

```typescript
interface FilterState {
  search: string;
  selectedTypes: string[];
  selectedTags: string[];
  selectedDate: string;
}
```

## Design Details

The component matches exact design specifications:

- **Colors:**
  - Background: `#f8f9fa`
  - Card background: `#ffffff`
  - Primary text: `#1a1a1a`
  - Secondary text: `#6c757d`
  - Links: `#0066cc`
  - Borders: `#dee2e6`

- **Spacing:**
  - Card padding: `24px`
  - Grid gap: `24px`
  - Border radius: `8px` (cards), `6px` (inputs)

- **Typography:**
  - Font family: `system-ui, -apple-system, sans-serif`
  - Heading: `20px`, weight `600`
  - Body: `15px`, weight `400`

## Tech Stack

- **React 18.3** - UI library
- **TypeScript 5.3** - Type safety
- **Vite 5.0** - Build tool and dev server
- **Jest 29.7** - Testing framework
- **React Testing Library 14.2** - Component testing
- **Lucide React** - Icon library

## Browser Support

- Chrome (latest)
- Firefox (latest)
- Safari (latest)
- Edge (latest)

## License

MIT

## Author

Generated by AI-powered workflow using Claude, with comprehensive testing and documentation.

---

**Repository:** https://github.com/leongroth/ClaudeSandbox

**Questions?** Open an issue on GitHub!
